import pandas as pd
from texblob import TextBlob

def add_sentiment(dff, text_col):
    # placeholder function - calculate if team crossed timezones with <48 hours rest
    return 1 if row['back_to_back'] and row ['crossed_timezones'] else 0

